import ReportExportPanel from "../components/ReportExportPanel";

/** Dedicated municipal reports — HTML preview, print, and PDF download. */
export default function ReportsPage() {
  return (
    <div className="page-reports">
      <ReportExportPanel />
    </div>
  );
}
